spring:
  application:
    name: seapedia-api

  datasource:
    url: jdbc:postgresql://localhost:5432/seapedia_db
    username: postgres
    password: yamabuki

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration