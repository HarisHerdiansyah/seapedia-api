meta {
  name: [Negative] Select Role — ADMIN Ambil BUYER
  type: http
  seq: 8
}

post {
  url: {{base_url}}/api/authentication/non-admin-role
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "activeRole": "BUYER"
  }
}

tests {
  test("Status is 403 Forbidden", function() {
    expect(res.getStatus()).to.equal(403);
  });

  test("success is false", function() {
    expect(res.getBody().success).to.equal(false);
  });

  test("message mentions admin restriction", function() {
    expect(res.getBody().message).to.include("Admin users");
  });
}
